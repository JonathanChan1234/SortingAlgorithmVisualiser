export interface SortingResult {
    sortedArray: number[];
    immediateResult: number[][];
    animations: number[][];
    algorithm: string;
}

export interface SortElements {
    sortArray: number[];
    animations: number[];
}